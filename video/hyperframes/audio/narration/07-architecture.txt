The evidence contract is simple: data becomes policy, policy meets physics, and every result becomes proof. Demonstrations are converted into a standard dataset, the policy is trained or fine-tuned on AMD, the simulator executes the action loop, and the evaluator records stage failures. The final package is not a screenshot. It is code, a command, a video, a JSON record, and a hash.
